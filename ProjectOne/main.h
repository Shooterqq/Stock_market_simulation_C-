#pragma once
#include <random>
#include <chrono>
#include <memory>
#include <unordered_map>
#include <iostream>
#include <unordered_map>
#include <string>
#include <random>
#include <algorithm>

typedef enum
{
    MENU,
    CREATE_ACCOUNT,
    CHANGE_ACCOUNT,
    SHOW_ACCOUNTS,
    MANAGE_ACCOUNT,
    SHOW_HISTORY,
    NEXT_TOUR,
    PLOT_CHART,
    EXIT
}sm_main;














