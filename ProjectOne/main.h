#pragma once
#include <random>
#include <chrono>
#include <memory>

typedef enum
{
    MENU,
    CREATE_ACCOUNT,
    SHOW_ACCOUNTS,
    MANAGE_ACCOUNT,
    SHOW_HISTORY,
    NEXT_TOUR,
    EXIT
}sm_main;


void add(int a, int b);













